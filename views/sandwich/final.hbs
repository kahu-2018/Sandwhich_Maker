<p> Wow, you must be some kind of sandcih wizard! What a combo!</p>

<p>A delicious {{protein.name}} patty smothered in melted {{cheese.name}} with locally sourced {{veg.name}} all wrapped 
  in a freshly baked {{bread.name}} bun. Yummy!</p>
<div class ='container'>
  <img src="{{bread.image}}">
  <img src="{{cheese.image}}">
  <img src="{{veg.image}}">
  <img src="{{protein.image}}">
</div>
{{!-- var proteinObj = proteinArray.find(function(item) {
  return proteinId == item.id
})
var breadObj = breadArray.find(function(item) {
  return breadId == item.id
})
var cheeseObj = cheeseArray.find(function(item) {
  return cheeseId == item.id
})
var vegObj = vegArray.find(function(item) {
  return vegId == item.id
}) --}}